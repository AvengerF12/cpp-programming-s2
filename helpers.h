#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

vector<int>::const_iterator move_pos(const vector<int>& vect, unsigned int target, char direction);
